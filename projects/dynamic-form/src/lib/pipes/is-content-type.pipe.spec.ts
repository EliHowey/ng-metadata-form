import { IsContentTypePipe } from './is-content-type.pipe';

describe('IsContentTypePipe', () => {
    it('create an instance', () => {
        const pipe = new IsContentTypePipe();
        expect(pipe).toBeTruthy();
    });
});
